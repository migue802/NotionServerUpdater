# Server name to identify in Notion database
SERVER_NAME=Test Entry

# Notion API Token (replace with your actual token)
# NOTION_TOKEN=ntn_your_actual_token_here

# Refresh period in seconds (default: 30)
REFRESH_PERIOD_SECONDS=30

# Optional: Node.js environment
NODE_ENV=production

# Optional: Logging level
LOG_LEVEL=info